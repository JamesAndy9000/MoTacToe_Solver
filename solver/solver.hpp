#ifndef SOLVER_HPP
#define SOLVER_HPP

#include "mtt_board.hpp"
#include <unordered_set>
#include <stack>

class Solver
{
	private:
		std::unordered_set<Position> possibleMoves;
	
	public:
		Solver();
		
		
};


#endif
