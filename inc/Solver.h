#ifndef SOLVER_H
#define SOLVER_H

#include "MTT_Board.h"
#include <unordered_set>
#include <stack>

class Solver
{
	private:
		std::unordered_set<Position> possibleMoves;
	
	public:
		Solver();
		
		
};


#endif
